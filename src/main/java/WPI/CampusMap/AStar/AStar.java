package WPI.CampusMap.AStar;

import WPI.CampusMap.Backend.Map;
import WPI.CampusMap.Backend.Point;

/**
 * 
 * @author Jacob Zizmor
 *
 */
public class AStar {
	
	/**
	 * Runs A* on a single map
	 * @param amap map to run A*
	 * @param start the starting Point on amap
	 * @param goal the goal Point on amap
	 * @return a path between start and goal
	 */
	public Path AStar(Map amap, Point start, Point goal) {
		return null;
	}
	
	/**
	 * Runs A* across multiple maps
	 * @param startMap the map of the starting Point
	 * @param goalMap the map of the goal Point
	 * @param start the starting Point located on startMap
	 * @param Goal the goal Point located on goalMap
	 * @return an array of Paths where each Path is a complete Path across a single Map
	 */
	public Path[] AStar(Map startMap, Map goalMap, Point start, Point Goal) {
		return null;
	}

}
